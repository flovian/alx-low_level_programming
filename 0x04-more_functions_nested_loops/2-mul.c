#include "main.h"
/**
 * mul - function that prints the multples of two numbers
 * @a: the int for the paramaters of the function
 * @b: the int for the paramaters of the function
 * Return: 0
 */
int mul(int a, int b)
{
	return (a * b);
}
